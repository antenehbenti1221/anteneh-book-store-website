document.addEventListener('click',function(e){const title=e.target.closest('#grid .book .title');if(!title)return;e.preventDefault();e.stopImmediatePropagation();const card=title.closest('.book');const wasOpen=card.classList.contains('expanded');document.querySelectorAll('#grid .book').forEach(c=>{c.classList.remove('expanded');const p=c.querySelector('.book-preview');if(p)p.hidden=true});if(!wasOpen){card.classList.add('expanded');const p=card.querySelector('.book-preview');if(p)p.hidden=false}},true);